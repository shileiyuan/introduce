// const prefix = '/api'

const API = {
  query_app: '/query/app',
  query_userList: '/query/userList',
  login: '/login'
}

// const keys = Object.keys(API)

// keys.forEach(key => {
//   API[key] = prefix + API[key]
// })

export default API
